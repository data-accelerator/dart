# Go build/test cache dirs (kept inside workspace for sandboxed runs)
/.gocache/
/.gopath/
/.gotmp/
/.toolchain/

# Binaries
/bin/
*.test
*.out

# Editor/OS
.DS_Store

# Local experiment rig (deploy/local-cluster.sh)
.dart-local/

# Working design notes, kept out of the repository
/design/

# Local multi-module workspace files: providers/k8s pins its sibling dependency
# with a replace directive, so a go.work is only ever a local convenience.
/go.work
/go.work.sum
